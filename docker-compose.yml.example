version: '3.3'
services:

  #PHP Service
  app:
    build:
      context: .
      dockerfile: ./.docker/Dockerfile
    container_name: provision-provider-shared-hosting-lib
    restart: unless-stopped
    tty: true
    environment:
      SERVICE_NAME: app
      SERVICE_TAGS: dev
    working_dir: /usr/src/provision-provider-shared-hosting-lib
    volumes:
      - ./:/usr/src/provision-provider-shared-hosting-lib
    networks:
      - app-network

#Docker Networks
networks:
  app-network:
    driver: bridge
